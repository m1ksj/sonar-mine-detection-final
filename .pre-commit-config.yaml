repos:
  - repo: local
    hooks:
      - id: run-unittests
        name: Run unittests
        entry: python -m unittest discover tests
        language: system
        pass_filenames: false

      - id: flake8
        name: flake8
        entry: flake8
        language: system
        types: [python]
